org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Set org.gradle.java.home in your local ~/.gradle/gradle.properties or via JAVA_HOME
# if the default JDK does not match the project's toolchain (JDK 17-21).
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# Mapbox secret token: put real value in ~/.gradle/gradle.properties, NOT here
# SDK_REGISTRY_TOKEN=sk.eyJ1...
